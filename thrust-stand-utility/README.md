# What you need

1. Python 3 with venv and pip installed
2. Programmable power supply connected via USB
3. Arduino connected via USB

## Create virtual environment with dependencies
```bash
python3 -m venv .env
source .env/bin/activate
pip install -r requirements.txt
```